services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  lattice:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      REDIS_URL: redis://redis:6379
      PUSH_SECRET: ${PUSH_SECRET:-dev-push-secret}
    depends_on:
      - redis

  ngrok:
    image: ngrok/ngrok:latest
    restart: unless-stopped
    command: http lattice:3000
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    ports:
      - "4040:4040"
    depends_on:
      - lattice
